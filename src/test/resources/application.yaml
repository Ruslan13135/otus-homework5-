Spring:
  datasource:
    url: jdbc:h2:mem:testdbt;MODE=MySQL;DATABASE_TO_LOWER=TRUE
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always
      data-locations: classpath:testdata.sql